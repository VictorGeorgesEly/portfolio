import {
  Typography,
} from "@mui/material";
import Wrapper from "../components/Wrapper";

export default function CV(): JSX.Element {
  return (
    <Wrapper title="Curriculum Vitae">
      <Typography variant="h1" gutterBottom>
        Work in progress
      </Typography>
    </Wrapper>
  );
}
